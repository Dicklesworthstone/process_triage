# Auto-limit CPU for debug builds to prevent runaway processes
[target.x86_64-unknown-linux-gnu]
runner = ["nice", "-n", "19"]

# Musl targets for static linking
[target.x86_64-unknown-linux-musl]
rustflags = ["-Z", "threads=4", "-C", "target-feature=+crt-static"]

[target.aarch64-unknown-linux-musl]
rustflags = ["-Z", "threads=4", "-C", "target-feature=+crt-static"]

# Also limit build parallelism
[build]
rustflags = ["-Z", "threads=4"]
jobs = 4
